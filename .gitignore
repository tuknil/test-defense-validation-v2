# Runtime run ledger (durable, but not source)
api/data/

# Local secrets / env (never commit tokens)
.env
*.env
!.env.example

# Editor swap files
*.swp

# JetBrains IDE workspace
.idea/

# Compiled API binaries (~27MB each; two were committed by accident)
api/defense-validation-api
api/mitigation-check-api
api/dvapi
